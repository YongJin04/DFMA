<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinUiApp.Pages.ArtifactsAnalysis.CaseImformation"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUiApp.Pages.ArtifactsAnalysis"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>

        <Border
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Padding="24"
            CornerRadius="12"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1">

            <StackPanel Spacing="16">

                <!-- 제목 -->
                <TextBlock
                    Text="케이스 정보"
                    FontSize="24"
                    FontWeight="SemiBold"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,4" />

                <!-- 설명 -->
                <TextBlock
                    Text="EWF 관련 라이브러리를 로드하고 버전 정보를 확인합니다."
                    Opacity="0.8"
                    FontSize="14"
                    TextAlignment="Center"
                    TextWrapping="Wrap"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,8" />

                <!-- 버튼 -->
                <Button
                    Content="libewf.dll 로드"
                    Width="180"
                    Height="40"
                    HorizontalAlignment="Center"
                    Click="LibewfLoadButton_Click" />

                <!-- 상태 텍스트 -->
                <TextBlock
                    x:Name="LibewfStatusTextBlock"
                    Text="아직 libewf.dll 을 로드하지 않았습니다."
                    TextAlignment="Center"
                    TextWrapping="Wrap"
                    FontSize="13"
                    HorizontalAlignment="Center"
                    MaxWidth="360"
                    Margin="0,8,0,0" />

            </StackPanel>
        </Border>
    </Grid>
</Page>
